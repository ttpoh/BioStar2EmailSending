extends layout
block content
    script(type='text/javascript').
        var baseInfo = !{JSON.stringify(baseInfo)};
    //-     consol.log('pug baseInfo', baseInfo)
    //- script(src="/../js/base/base.js")
    link(rel="stylesheet", href="/stylesheet/biostarSetting.css")
   
    .content
        .window_left
            .file-container(id="filesButton-container")                           
        .window_right
            .login-container
                form.form(action="/login", method="POST")
                    //- biostar2にログインするための情報
                    .bs2-container
                        h4 BioStar 2 ログイン情報
                        .flex-box
                            //- label(for="url") BIOSTAR_URL
                            input(type="text", id="url" name="url",  placeholder="biostar2 URL", value = baseInfo.User.url)
                        .flex-box
                            //- label(for="user_id")
                            input(type="text", id="user_id", name="user_id", placeholder="ID", value = baseInfo.User.login_id)
                        .flex-box
                            //- label(for="password") パスワード
                            input(type="password",id="password" name="password", placeholder="Password", value = baseInfo.User.password)
                        button(type="submit", id="basesave", name="basesave") 設定保存            
            
                        footer © 2024 SECURE Inc.  All rights reserved.
